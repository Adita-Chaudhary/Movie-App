MovieNest — a movie discovery app built with React, Vite, and the TMDB API.

The app itself lives in [`my-react-vite-app/`](./my-react-vite-app) — see [`my-react-vite-app/README.md`](./my-react-vite-app/README.md) for features, setup, architecture, the recommendation algorithm, and deployment notes.
